{
	"version": 3,
	"configurePresets": [
		{
			"name": "base",
			"hidden": true,
			"binaryDir": "${sourceDir}/build",
			"cacheVariables": {
				"IP_INVENTORY_CONFIGURED_FROM_PRESET": "ON"
			}
		},
		{
			"name": "debug-base",
			"hidden": true,
			"inherits": "base",
			"cacheVariables": {
				"CMAKE_BUILD_TYPE": "Debug",
				"IP_INVENTORY_ASSERT_ENABLED": "ON"
			}
		},
		{
			"name": "release-base",
			"hidden": true,
			"inherits": "base",
			"cacheVariables": {
				"CMAKE_BUILD_TYPE": "Release",
				"IP_INVENTORY_ASSERT_ENABLED": "OFF"
			}
		},

		{
			"name": "clang-debug",
			"displayName": "Clang debug configuration.",
			"description": "Debug build configured with the native Clang toolchain.",
			"inherits": "debug-base",
			"toolchainFile": "${sourceDir}/cmake/toolchains/clang-native.cmake"
		},
		{
			"name": "clang-release",
			"displayName": "Clang release configuration.",
			"description": "Release build configured with the native Clang toolchain.",
			"inherits": "release-base",
			"toolchainFile": "${sourceDir}/cmake/toolchains/clang-native.cmake"
		},
		{
			"name": "gcc-debug",
			"displayName": "Gcc debug configuration.",
			"description": "Debug build configured with the native gcc toolchain.",
			"inherits": "debug-base",
			"toolchainFile": "${sourceDir}/cmake/toolchains/gcc-native.cmake"
		},
		{
			"name": "gcc-release",
			"displayName": "Gcc release configuration.",
			"description": "Release build configured with the native gcc toolchain.",
			"inherits": "release-base",
			"toolchainFile": "${sourceDir}/cmake/toolchains/gcc-native.cmake"
		},
		{
			"name": "msvc-debug",
			"displayName": "MSVC debug configuration.",
			"description": "Debug build configured with the native MSVC toolchain.",
			"inherits": "debug-base",
			"toolchainFile": "${sourceDir}/cmake/toolchains/msvc-native.cmake"
		},
		{
			"name": "msvc-release",
			"displayName": "MSVC release configuration.",
			"description": "Release build configured with the native MSVC toolchain.",
			"inherits": "release-base",
			"toolchainFile": "${sourceDir}/cmake/toolchains/msvc-native.cmake"
		}
	]
}
